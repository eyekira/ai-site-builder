# npm registry URL should be provided securely (e.g., Replit Secrets / CI variables)
registry=${MIRROR_URL}

# Always send auth when talking to the private/company mirror
always-auth=true

# Replace YOUR_MIRROR_HOST with the host part of MIRROR_URL (no protocol)
# Example: if MIRROR_URL=https://npm.company.internal/repository/npm/, use:
# //npm.company.internal/:_authToken=${NPM_TOKEN}
//YOUR_MIRROR_HOST/:_authToken=${NPM_TOKEN}
